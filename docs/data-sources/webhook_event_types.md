---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "featureflip_webhook_event_types Data Source - featureflip"
subcategory: ""
description: |-
  The event types a featureflip_webhook can filter on. Requires an Admin token.
---

# featureflip_webhook_event_types (Data Source)

The event types a `featureflip_webhook` can filter on. Requires an Admin token.

## Example Usage

```terraform
data "featureflip_webhook_event_types" "all" {}

# Subscribe to every flag event, but nothing else.
resource "featureflip_webhook" "flags_only" {
  name        = "flags-only"
  url         = "https://hooks.example.com/featureflip"
  event_types = [for t in data.featureflip_webhook_event_types.all.event_types : t if startswith(t, "flag.")]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Read-Only

- `event_types` (List of String)
