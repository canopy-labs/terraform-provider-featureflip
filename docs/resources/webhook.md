---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "featureflip_webhook Resource - featureflip"
subcategory: ""
description: |-
  An outbound webhook subscription: where Featureflip sends configuration-change events for the provider's organization. Requires an Admin token.
  The first signing secret is only available at creation and is stored in Terraform state — protect your state. To rotate it, add a featureflip_webhook_secret. Import with the webhook id; imported webhooks cannot recover the secret.
  Featureflip disables a subscription whose deliveries keep failing (auto_disabled_at is set). Because enabled defaults to true, the next plan shows enabled changing back to true, and applying it re-enables the subscription and resets its failure count. Fix the receiver first.
---

# featureflip_webhook (Resource)

An outbound webhook subscription: where Featureflip sends configuration-change events for the provider's organization. Requires an Admin token.

The first signing secret is only available at creation and is stored in Terraform state — protect your state. To rotate it, add a `featureflip_webhook_secret`. Import with the webhook id; imported webhooks cannot recover the secret.

Featureflip disables a subscription whose deliveries keep failing (`auto_disabled_at` is set). Because `enabled` defaults to `true`, the next plan shows `enabled` changing back to `true`, and applying it re-enables the subscription and resets its failure count. Fix the receiver first.

## Example Usage

```terraform
# Send flag changes in the web project's production environment to a receiver.
# Omit event_types, project_ids or environment_ids to receive everything.
resource "featureflip_webhook" "flag_changes" {
  name            = "flag-changes"
  url             = "https://hooks.example.com/featureflip"
  event_types     = ["flag.toggled", "flag.updated", "flag.environment_config.updated"]
  project_ids     = [featureflip_project.web.id]
  environment_ids = [featureflip_environment.production.id]
}

# The signing secret is only available at creation and lives in Terraform state.
output "webhook_signing_secret" {
  value     = featureflip_webhook.flag_changes.secret
  sensitive = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String)
- `url` (String) Receiver URL. Must be https and must not point at a private, loopback, link-local or reserved address. If it embeds a credential, pass it through a sensitive variable.

### Optional

- `enabled` (Boolean)
- `environment_ids` (Set of String) Environment ids (`featureflip_environment.<name>.id`, not keys) to send events for. Omit to cover every environment.
- `event_types` (Set of String) Event types to send, e.g. `flag.toggled`. Omit (or leave empty) to receive every event type. The `featureflip_webhook_event_types` data source lists the valid values.
- `project_ids` (Set of String) Project ids (`featureflip_project.<name>.id`, not keys) to send events for. Omit to cover every project.
- `type` (String) Receiver type (the API's `provider` field, renamed because Terraform reserves that name). Only `GenericHttp` is available. Immutable.

### Read-Only

- `auto_disabled_at` (String) When Featureflip disabled the subscription after repeated delivery failures; null otherwise.
- `id` (String) The ID of this resource.
- `secret` (String, Sensitive) The signing secret created with the subscription (creation-time only; null for imported webhooks).
- `secret_id` (String) Id of the signing secret created with the subscription. Use it to adopt that secret into a `featureflip_webhook_secret` when rotating it out.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Import by webhook id. The signing secret cannot be recovered on import.
terraform import featureflip_webhook.flag_changes 0199a8c4-5b1e-7c2d-9f3a-4e6b8d0c1a2f
```
