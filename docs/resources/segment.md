---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "featureflip_segment Resource - featureflip"
subcategory: ""
description: |-
  A reusable user segment. Import with <project>/<segment-key>.
---

# featureflip_segment (Resource)

A reusable user segment. Import with `<project>/<segment-key>`.

## Example Usage

```terraform
resource "featureflip_segment" "beta_users" {
  project = featureflip_project.web.key
  key     = "beta-users"
  name    = "Beta Users"

  conditions = [
    { attribute = "email", operator = "EndsWith", values = ["@acme.io"] },
  ]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `key` (String)
- `name` (String)
- `project` (String)

### Optional

- `conditions` (Attributes List) (see [below for nested schema](#nestedatt--conditions))
- `description` (String)

### Read-Only

- `id` (String) The ID of this resource.

<a id="nestedatt--conditions"></a>
### Nested Schema for `conditions`

Required:

- `attribute` (String) User attribute to match, e.g. country or email.
- `operator` (String)
- `values` (List of String)

Optional:

- `negate` (Boolean)

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import featureflip_segment.beta_users web/beta-users
```
